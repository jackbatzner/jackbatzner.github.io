<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://jackbatzner.github.io/feed.xml" rel="self" type="application/atom+xml" /><link href="https://jackbatzner.github.io/" rel="alternate" type="text/html" hreflang="en" /><updated>2026-04-08T11:12:58-05:00</updated><id>https://jackbatzner.github.io/feed.xml</id><title type="html">Jack Batzner</title><subtitle>Engineering, AI agents, and building things that actually work.</subtitle><entry><title type="html">My Agent Reviewed My Teammate’s Code. I Had No Idea.</title><link href="https://jackbatzner.github.io/2026/04/08/agent-guardrails/" rel="alternate" type="text/html" title="My Agent Reviewed My Teammate’s Code. I Had No Idea." /><published>2026-04-08T01:00:00-05:00</published><updated>2026-04-08T01:00:00-05:00</updated><id>https://jackbatzner.github.io/2026/04/08/agent-guardrails</id><content type="html" xml:base="https://jackbatzner.github.io/2026/04/08/agent-guardrails/"><![CDATA[<p>I’m running a squad of GitHub Copilot agents to automate my team’s daily engineering operations. They triage bugs, monitor incidents, post digests — the whole thing runs on the <a href="https://bradygaster.github.io/squad/">Squad framework</a>, a multi-agent system where each agent is defined in markdown, knows its role, and works autonomously in a loop.</p>

<p>It’s genuinely one of the most useful things I’ve built.</p>

<p>Yesterday, one of my agents left inline code review comments on my teammate’s pull request. Comments I hadn’t seen. Comments I hadn’t approved. Comments I didn’t write, from a session I hadn’t started.</p>

<p>My teammate pinged me about the feedback. I had no idea what they were talking about.</p>

<h2 id="what-happened">What Happened</h2>

<p>I built a code review agent. It reads open PRs, runs a structured analysis — logic, security, test coverage — and produces findings. Exactly what I wanted.</p>

<p>What I didn’t want: it acting on those findings without me.</p>

<p>The GitHub MCP includes a <code class="language-plaintext highlighter-rouge">create_review_comment</code> tool. The agent’s instructions said <em>“review pull requests and surface findings.”</em> The most direct path from A to B is to comment on the PR. So it did. Directly. On a PR my teammate was actively waiting on for human feedback.</p>

<p>The model wasn’t wrong. It was following its instructions, using available tools, taking the most direct path to done. That’s what models do.</p>

<p>The problem was mine. I gave it the tools. I gave it vague instructions. I didn’t give it a boundary.</p>

<h2 id="the-broader-problem">The Broader Problem</h2>

<p>This applies to any GitHub Copilot agent setup — whether you’re using the <a href="https://bradygaster.github.io/squad/">Squad framework</a>, defining custom agents with <a href="https://docs.github.com/en/copilot/how-tos/use-copilot-agents/coding-agent/create-custom-agents"><code class="language-plaintext highlighter-rouge">.github/agents/*.agent.md</code> files</a>, scoping behavior via <code class="language-plaintext highlighter-rouge">.github/copilot-instructions.md</code>, or rolling your own.</p>

<p>The moment you wire up an MCP server, you’re giving your agent real capabilities with real consequences. The <a href="https://devblogs.microsoft.com/devops/azure-devops-remote-mcp-server-public-preview/">Azure DevOps Remote MCP Server</a> — now in public preview — is a good example: one entry in your <code class="language-plaintext highlighter-rouge">mcp.json</code> and your agent can read work items, create branches, update PRs, and comment on code across your entire ADO organization. The GitHub MCP has had similar reach since day one.</p>

<p>And it doesn’t stop there. Teams MCPs can send messages. Email MCPs can draft and send mail. Calendar MCPs can create meetings. Work intelligence tools can update records across your org. Every MCP you add is another surface where an agent can take a real-world action — and without explicit constraints, it will use every one of them to get to done as directly as possible.</p>

<p>MCP tools don’t have opinions about when they should be used. They have descriptions. The agent reads its task, reads the available tools, and picks the most direct path.</p>

<p>Without explicit constraints, <em>your job as the author is being done by inference.</em> The agent decides who to notify, where to post, which actions are appropriate, what counts as done. It will usually infer correctly. Occasionally it won’t. And when it doesn’t, something real happened in the world before you found out.</p>

<h2 id="the-fix-defense-in-depth">The Fix: Defense in Depth</h2>

<p>One constraint isn’t enough. Three layers working together are.</p>

<h3 id="layer-1--agent-instructions">Layer 1 — Agent Instructions</h3>

<p>GitHub Copilot reads instruction files before every task. The right file depends on your setup:</p>

<ul>
  <li><strong><code class="language-plaintext highlighter-rouge">.github/copilot-instructions.md</code></strong> — repo-wide, applied to all Copilot modes</li>
  <li><strong><code class="language-plaintext highlighter-rouge">.github/agents/*.agent.md</code></strong> — per-agent definitions with scoped behavior</li>
  <li><strong><code class="language-plaintext highlighter-rouge">.github/instructions/*.instructions.md</code></strong> — path-scoped with <code class="language-plaintext highlighter-rouge">applyTo</code> frontmatter</li>
</ul>

<p>Whatever format you use, put scope boundaries and forbidden actions at the top — explicitly, not implicitly:</p>

<figure class="highlight"><pre><code class="language-markdown" data-lang="markdown"><span class="gu">## Scope</span>
<span class="p">-</span> Repository: this repo only — do not act on any other repo, even if accessible
<span class="p">-</span> PR access: read-only — never write to PRs without explicit human dispatch

<span class="gu">## Never Do This</span>
<span class="p">-</span> ❌ Post inline PR comments — write findings to the tracking issue only
<span class="p">-</span> ❌ Approve or request changes on a PR
<span class="p">-</span> ❌ Merge PRs
<span class="p">-</span> ❌ Contact or @mention anyone outside the team
<span class="p">-</span> ❌ Close or delete issues without human confirmation</code></pre></figure>

<p>Explicit beats implicit. <em>“Surface findings”</em> leaves room for inference. <em>“Never post inline PR comments”</em> does not.</p>

<p>This is necessary but not sufficient. It’s a model-level instruction — models can reason their way around instructions. You need the next layer.</p>

<h3 id="layer-2--prepost-tool-hooks">Layer 2 — Pre/Post Tool Hooks</h3>

<p>GitHub Copilot supports <a href="https://docs.github.com/en/copilot/how-tos/use-copilot-agents/coding-agent/use-hooks">pre- and post-tool call hooks</a> via <code class="language-plaintext highlighter-rouge">.github/hooks/guardrails.json</code> — also available in <a href="https://code.visualstudio.com/docs/copilot/customization/hooks">VS Code</a>:</p>

<figure class="highlight"><pre><code class="language-json" data-lang="json"><span class="p">{</span><span class="w">
  </span><span class="nl">"version"</span><span class="p">:</span><span class="w"> </span><span class="mi">1</span><span class="p">,</span><span class="w">
  </span><span class="nl">"hooks"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
    </span><span class="nl">"preToolUse"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
      </span><span class="p">{</span><span class="w">
        </span><span class="nl">"type"</span><span class="p">:</span><span class="w"> </span><span class="s2">"command"</span><span class="p">,</span><span class="w">
        </span><span class="nl">"powershell"</span><span class="p">:</span><span class="w"> </span><span class="s2">"./.github/hooks/scripts/pre-tool-guard.ps1"</span><span class="p">,</span><span class="w">
        </span><span class="nl">"bash"</span><span class="p">:</span><span class="w"> </span><span class="s2">"./.github/hooks/scripts/pre-tool-guard.sh"</span><span class="p">,</span><span class="w">
        </span><span class="nl">"timeoutSec"</span><span class="p">:</span><span class="w"> </span><span class="mi">10</span><span class="w">
      </span><span class="p">}</span><span class="w">
    </span><span class="p">],</span><span class="w">
    </span><span class="nl">"postToolUse"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
      </span><span class="p">{</span><span class="w">
        </span><span class="nl">"type"</span><span class="p">:</span><span class="w"> </span><span class="s2">"command"</span><span class="p">,</span><span class="w">
        </span><span class="nl">"powershell"</span><span class="p">:</span><span class="w"> </span><span class="s2">"./.github/hooks/scripts/post-tool-audit.ps1"</span><span class="p">,</span><span class="w">
        </span><span class="nl">"bash"</span><span class="p">:</span><span class="w"> </span><span class="s2">"./.github/hooks/scripts/post-tool-audit.sh"</span><span class="p">,</span><span class="w">
        </span><span class="nl">"timeoutSec"</span><span class="p">:</span><span class="w"> </span><span class="mi">10</span><span class="w">
      </span><span class="p">}</span><span class="w">
    </span><span class="p">]</span><span class="w">
  </span><span class="p">}</span><span class="w">
</span><span class="p">}</span></code></pre></figure>

<p>Before <em>any</em> tool call executes, your pre-tool script fires. It reads the tool name and arguments, checks them against your rules, and returns a block decision if something violates scope. The runtime respects the deny — the tool call never reaches the external system.</p>

<p>Here’s the pattern that would have caught my incident:</p>

<figure class="highlight"><pre><code class="language-powershell" data-lang="powershell"><span class="kr">function</span><span class="w"> </span><span class="nf">Deny</span><span class="w"> </span><span class="p">{</span><span class="w">
    </span><span class="kr">param</span><span class="p">([</span><span class="n">string</span><span class="p">]</span><span class="nv">$Reason</span><span class="p">)</span><span class="w">
    </span><span class="p">@{</span><span class="w"> </span><span class="nx">permissionDecision</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">"deny"</span><span class="p">;</span><span class="w"> </span><span class="nx">permissionDecisionReason</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="nv">$Reason</span><span class="w"> </span><span class="p">}</span><span class="w"> </span><span class="se">`</span><span class="w">
        </span><span class="o">|</span><span class="w"> </span><span class="n">ConvertTo-Json</span><span class="w"> </span><span class="nt">-Compress</span><span class="w">
    </span><span class="kr">exit</span><span class="w"> </span><span class="mi">0</span><span class="w">
</span><span class="p">}</span><span class="w">

</span><span class="c"># Block all PR comment creation — no exceptions.</span><span class="w">
</span><span class="kr">if</span><span class="w"> </span><span class="p">(</span><span class="nv">$toolName</span><span class="w"> </span><span class="o">-match</span><span class="w"> </span><span class="s2">"create_review_comment|create_pull_request_thread|reply_to_comment"</span><span class="p">)</span><span class="w"> </span><span class="p">{</span><span class="w">
    </span><span class="n">Deny</span><span class="w"> </span><span class="s2">"PR comments are blocked. Write findings to the tracking issue only."</span><span class="w">
</span><span class="p">}</span><span class="w">

</span><span class="c"># Block destructive operations</span><span class="w">
</span><span class="kr">if</span><span class="w"> </span><span class="p">(</span><span class="nv">$toolName</span><span class="w"> </span><span class="o">-match</span><span class="w"> </span><span class="s2">"merge_pull_request|delete_issue|delete_comment"</span><span class="p">)</span><span class="w"> </span><span class="p">{</span><span class="w">
    </span><span class="n">Deny</span><span class="w"> </span><span class="s2">"Destructive operations require a human. Escalate instead."</span><span class="w">
</span><span class="p">}</span><span class="w">

</span><span class="c"># Scope guard — only your repo, nothing else</span><span class="w">
</span><span class="kr">if</span><span class="w"> </span><span class="p">(</span><span class="nv">$toolName</span><span class="w"> </span><span class="o">-match</span><span class="w"> </span><span class="s2">"^github-mcp-server-"</span><span class="p">)</span><span class="w"> </span><span class="p">{</span><span class="w">
    </span><span class="kr">if</span><span class="w"> </span><span class="p">(</span><span class="bp">$args</span><span class="o">.</span><span class="nf">owner</span><span class="w"> </span><span class="o">-and</span><span class="w"> </span><span class="bp">$args</span><span class="o">.</span><span class="nf">owner</span><span class="w"> </span><span class="o">-ne</span><span class="w"> </span><span class="nv">$</span><span class="nn">env</span><span class="p">:</span><span class="nv">ALLOWED_GH_OWNER</span><span class="p">)</span><span class="w"> </span><span class="p">{</span><span class="w">
        </span><span class="n">Deny</span><span class="w"> </span><span class="s2">"Owner '</span><span class="si">$(</span><span class="bp">$args</span><span class="o">.</span><span class="nf">owner</span><span class="si">)</span><span class="s2">' is outside the allowed scope."</span><span class="w">
    </span><span class="p">}</span><span class="w">
    </span><span class="kr">if</span><span class="w"> </span><span class="p">(</span><span class="bp">$args</span><span class="o">.</span><span class="nf">repo</span><span class="w"> </span><span class="o">-and</span><span class="w"> </span><span class="bp">$args</span><span class="o">.</span><span class="nf">repo</span><span class="w"> </span><span class="o">-ne</span><span class="w"> </span><span class="nv">$</span><span class="nn">env</span><span class="p">:</span><span class="nv">ALLOWED_GH_REPO</span><span class="p">)</span><span class="w"> </span><span class="p">{</span><span class="w">
        </span><span class="n">Deny</span><span class="w"> </span><span class="s2">"Repo '</span><span class="si">$(</span><span class="bp">$args</span><span class="o">.</span><span class="nf">repo</span><span class="si">)</span><span class="s2">' is outside the allowed scope."</span><span class="w">
    </span><span class="p">}</span><span class="w">
</span><span class="p">}</span></code></pre></figure>

<p>The post-tool hook closes the loop — log every write operation, alert on anything high-risk that succeeds:</p>

<figure class="highlight"><pre><code class="language-powershell" data-lang="powershell"><span class="nv">$HIGH_RISK_TOOLS</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="p">@(</span><span class="s2">"merge_pull_request"</span><span class="p">,</span><span class="w"> </span><span class="s2">"delete_issue"</span><span class="p">,</span><span class="w"> </span><span class="s2">"delete_comment"</span><span class="p">)</span><span class="w">

</span><span class="kr">if</span><span class="w"> </span><span class="p">(</span><span class="nv">$HIGH_RISK_TOOLS</span><span class="w"> </span><span class="o">|</span><span class="w"> </span><span class="n">Where-Object</span><span class="w"> </span><span class="p">{</span><span class="w"> </span><span class="nv">$toolName</span><span class="w"> </span><span class="o">-match</span><span class="w"> </span><span class="bp">$_</span><span class="w"> </span><span class="p">})</span><span class="w"> </span><span class="p">{</span><span class="w">
    </span><span class="n">Add-Content</span><span class="w"> </span><span class="s2">".agent/audit.jsonl"</span><span class="w"> </span><span class="p">(</span><span class="nv">$auditEntry</span><span class="w"> </span><span class="o">|</span><span class="w"> </span><span class="n">ConvertTo-Json</span><span class="w"> </span><span class="nt">-Compress</span><span class="p">)</span><span class="w">
</span><span class="p">}</span></code></pre></figure>

<p>This is your audit trail. When something unexpected happens, you know exactly which agent called which tool at what time. Without it you’re debugging with no provenance.</p>

<h3 id="layer-3--human-dispatch-gates">Layer 3 — Human Dispatch Gates</h3>

<p>Some actions should structurally require a human before they happen — not “the model probably won’t without being asked” but “the infrastructure won’t allow it.”</p>

<p>In my setup, review agents write findings to a queue file. Nothing gets posted to a PR until I explicitly comment <code class="language-plaintext highlighter-rouge">dispatch</code> on a tracking issue. The pre-tool hook enforces this: if an agent tries to bypass the queue, the hook blocks the call and tells it exactly where findings should go.</p>

<p>Design your agents with explicit pause points. It’s not overhead — it’s what makes continuous automation trustworthy enough to actually run continuously.</p>

<h2 id="the-short-version">The Short Version</h2>

<p>Three layers. All three required:</p>

<table>
  <thead>
    <tr>
      <th>Layer</th>
      <th>What it does</th>
      <th>What it relies on</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Agent instructions</td>
      <td>Agent knows the rules</td>
      <td>Model following them</td>
    </tr>
    <tr>
      <td>Pre-tool hooks</td>
      <td>Infrastructure enforces them</td>
      <td>Runtime, not the model</td>
    </tr>
    <tr>
      <td>Post-tool audit + dispatch gates</td>
      <td>Full visibility + human approval</td>
      <td>Logs and explicit sign-off</td>
    </tr>
  </tbody>
</table>

<p>Any one alone is insufficient. Together they’re the difference between <em>trusting</em> your agents and <em>hoping</em> they behave.</p>

<h2 id="whats-next">What’s Next</h2>

<p>Everything here lives at the GitHub Copilot layer — instruction files, hooks, dispatch gates. That’s the right place to start, but it’s not the whole picture.</p>

<p>What if you’re building agents in Python, TypeScript, Go, or .NET — outside of Copilot entirely? The same problem exists. An agent that can call tools will call them. The boundary logic has to live somewhere, and “in the system prompt” is not a guarantee.</p>

<p>And then there’s the MCP layer itself. The tools your agent can reach aren’t just a property of your code — they’re a property of what’s been wired up and exposed. An MCP like <a href="https://learn.microsoft.com/en-us/microsoft-copilot-studio/use-work-iq">Work IQ</a> can give agents access to Teams channels, meetings, emails, and shared organizational context. That’s genuinely powerful. It’s also exactly the kind of surface where you want explicit controls on what agents can reach, not just instructions asking them to be careful.</p>

<p>Governing your agents means governing the code you write <em>and</em> the tools you expose. Both layers. All the way down.</p>

<p><em>More on that soon.</em></p>

<hr />

<p><strong>Resources</strong></p>
<ul>
  <li><a href="https://bradygaster.github.io/squad/">Squad framework</a></li>
  <li><a href="https://docs.github.com/en/copilot/how-tos/use-copilot-agents/coding-agent/create-custom-agents">GitHub Copilot custom agents</a></li>
  <li><a href="https://docs.github.com/en/copilot/how-tos/use-copilot-agents/coding-agent/use-hooks">GitHub Copilot agent hooks</a></li>
  <li><a href="https://code.visualstudio.com/docs/copilot/customization/hooks">VS Code agent hooks</a></li>
  <li><a href="https://devblogs.microsoft.com/devops/azure-devops-remote-mcp-server-public-preview/">Azure DevOps Remote MCP Server (public preview)</a></li>
  <li><a href="https://opensource.microsoft.com/blog/2026/04/02/introducing-the-agent-governance-toolkit-open-source-runtime-security-for-ai-agents/">Introducing the Agent Governance Toolkit</a></li>
  <li><a href="https://learn.microsoft.com/en-us/microsoft-copilot-studio/use-work-iq">Work IQ MCP — centralized org-level agent governance</a></li>
</ul>]]></content><author><name>Jack Batzner</name></author><category term="Agents" /><category term="Guardrails" /><category term="copilot" /><category term="mcp" /><category term="squad" /><category term="guardrails" /><category term="agents" /><summary type="html"><![CDATA[I’m running a squad of GitHub Copilot agents to automate my team’s daily engineering operations. They triage bugs, monitor incidents, post digests — the whole thing runs on the Squad framework, a multi-agent system where each agent is defined in markdown, knows its role, and works autonomously in a loop.]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="https://jackbatzner.github.io/assets/img/agent-guardrails-hero.png" /><media:content medium="image" url="https://jackbatzner.github.io/assets/img/agent-guardrails-hero.png" xmlns:media="http://search.yahoo.com/mrss/" /></entry></feed>